spring:
    application:
        name: vanta-consumer
    config:
        import: optional:nacos:consumer.yaml,classpath:fallback.yaml
    cloud:
        nacos:
            config:
                server-addr: 127.0.0.1:8848
                namespace: vanta
                group: TEST_GROUP
