spring:
    application:
        name: vanta-bootstrap
    config:
        import:
            - optional:nacos:bootstrap_1.yaml
    cloud:
        nacos:
            serverAddr: 127.0.0.1:8848
            config:
                namespace: vanta
                group: TEST_GROUP
            discovery:
                namespace: vanta
                group: TEST_GROUP
                metadata:
                    zone-id: s1
                port: ${network.port}
