spring:
    application:
        name: vanta-bootstrap
    config:
        import: optional:nacos:bootstrap_1.yaml,classpath:fallback.yaml
    cloud:
        nacos:
            config:
                server-addr: 127.0.0.1:8848
                namespace: vanta
                group: TEST_GROUP
