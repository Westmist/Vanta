spring:
    application:
        name: vanta-game
    config:
        import:
            - optional:nacos:game.yaml
    cloud:
        nacos:
            serverAddr: 127.0.0.1:8848
            config:
                namespace: vanta
                group: TEST_GROUP
            discovery:
                namespace: vanta
                group: TEST_GROUP
                metadata:
                    zone-id: s1
                port: ${network.port}
