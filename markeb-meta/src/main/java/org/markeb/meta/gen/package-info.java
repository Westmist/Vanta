/**
 * Luban 生成的配置表代码包
 *
 * 此包下的代码由 Luban 工具自动生成，请勿手动修改。
 *
 * 目录结构：
 * - beans/  配置 Bean 类（数据结构）
 * - tables/ 配置表类（数据容器）
 *
 * 生成命令示例：
 * dotnet Luban.dll \
 *   -t server \
 *   -c java-bin \
 *   -d bin \
 *   --conf luban.conf \
 *   -x outputCodeDir=src/main/java/org/markeb/meta/gen \
 *   -x outputDataDir=src/main/resources/meta
 */
package org.markeb.meta.gen;

